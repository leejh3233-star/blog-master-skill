# START HERE — NAVER BLOG MASTER v3.8

## 1. 가장 간단한 사용법

```text
$naver-blog-master AUTO FULL
지금 조회수를 최대한 노릴 수 있는 주제를 찾아서 글을 완성해줘.
```

특정 주제가 있으면:

```text
$naver-blog-master AUTO FULL
주제: 2026 추석 KTX 취소표
```

프롬프트를 길게 쓰지 않아도 됩니다.
스킬이 내부적으로 독자, 목적, 검색의도, 체류, 링크, 이미지, 팩트체크 조건을 정리합니다.

---

## 2. v3.8에서 가장 중요한 변화

**링크가 검증됐는지와 링크가 사용자에게 실제 보이는지는 별개로 검사합니다.**

이전에는 링크가 정상이어도 ChatGPT 글쓰기 블록에서 링크가 보이지 않거나 복사되지 않는 문제가 있었습니다.

이제는:

```text
링크 검색
↓
PASS 1 실제 오픈 검증
↓
PASS 2 전달 직전 재검증
↓
링크 전달 방식 결정
↓
USER-VISIBLE LINK GATE
↓
필요 시 COPYABLE EXPORT
↓
발행 직전 재확인
↓
FINAL PASS
```

으로 진행합니다.

---

## 3. 링크 전달 방식은 3가지입니다

### INLINE_VISIBLE
현재 화면에서 글쓰기 블록 안 링크가 실제로 보이고 클릭 가능한 경우.

### MARKER_PLUS_PANEL
현재 ChatGPT처럼 글쓰기 블록 링크가 불안정한 경우의 기본 방식입니다.

본문에는:

```text
[공식 링크 L1 — 근로장려금 신청 바로가기]
```

마커를 넣고, 글 바로 아래에는 같은 번호의 실제 클릭 가능한 링크 패널을 제공합니다.

마커만 있고 클릭 가능한 패널이 없으면 FAIL입니다.

### COPYABLE_EXPORT
네이버 편집기에 복사할 때 실제 URL 문자열까지 꼭 보존되어야 하면 가능한 환경에서 링크 포함 TXT/HTML 발행본을 따로 생성합니다.

LINK-CRITICAL 글에서 글쓰기 블록 raw URL 보존을 확신할 수 없으면 이 방식을 우선합니다.

---

## 4. LINK-CRITICAL MODE

다음 주제는 자동으로 링크 중요 모드가 켜집니다.

- 예매
- 신청
- 가입
- 예약
- 지도/길찾기
- 잔여석/재고
- 지원금/정책 신청
- 통신사·쇼핑 공식 신청
- 공식 방송/클립
- 독자가 직접 공식자료로 팩트체크해야 하는 글

필수 링크 하나라도 **검증 실패 또는 전달 실패**면 최종 승인하지 않습니다.

---

## 5. 링크는 무엇을 검증하나요?

필수 링크마다 확인합니다.

- 실제로 열리는가
- 404/오류/종료 페이지가 아닌가
- 공식 도메인인가
- 링크 라벨과 실제 페이지 목적이 같은가
- 현재 연도·시즌·제품·행사와 맞는가
- 오래된 프로모션이 아닌가
- 로그인/앱 전용 제한이 있는가
- 더 직접적인 공식 deep link가 있는가
- 발행 직전에도 유효한가

단순히 `페이지가 열린다`만으로 PASS하지 않습니다.

---

## 6. USER-VISIBLE LINK GATE

최종 출력에서 확인합니다.

- 링크 목적이 사용자 화면에 보이는가
- 실제 클릭 가능한 링크가 있는가
- 본문 L1/L2/L3 마커와 클릭 패널이 1:1로 맞는가
- 필수 링크가 전부 전달됐는가
- 내부 참조 데이터가 있다는 이유만으로 PASS하지 않았는가

---

## 7. 네이버에 링크까지 복사하고 싶다면

사용자가 다음처럼 요청하면:

```text
링크까지 포함해서 네이버에 그대로 복사할 수 있게 줘
```

현재 화면에서 URL 보존을 확실히 할 수 없는 경우 가능한 환경에서는 `naver-publish-ready.txt` 또는 `html` 복사용 파일을 만듭니다.

파일에는:
- 제목
- 본문
- 이미지 삽입 위치
- 링크 목적
- 실제 검증 URL
- 태그

를 포함합니다.

파일 생성이 불가능한 환경이면 `COPYABLE_URL_DELIVERY = LIMITED`라고 솔직하게 표시합니다.

---

## 8. 첫 응답에서 받아야 하는 것

AUTO FULL이 정상 실행되면 기본적으로 다음이 보여야 합니다.

1. 최종 선정 결과
2. 발행용 글쓰기 블록
3. 실제 클릭 가능한 LINK PANEL
4. 필요 시 COPYABLE PUBLISH EXPORT
5. 검증 링크팩
6. 이미지 제작팩
7. 요구사항 충족 요약 + 최종 QA
8. 이미지 큐 READY 상태

---

## 9. 검수 구조

```text
담당자 작성
↓
Reviewer
↓
Approval Gate
↓
FAIL이면 해당 단계로 반려
↓
링크 실제 오픈 검증
↓
링크 전달 방식 확인
↓
USER-VISIBLE LINK GATE
↓
필요 시 COPYABLE LINK GATE
↓
Final Control Tower
```

---

## 10. 글과 이미지 단계는 분리

글 요청을 하면 첫 응답에서 글이 먼저 완성됩니다.

```text
조사
↓
검수
↓
링크 검증·전달 확인
↓
최종 승인
↓
글쓰기 블록 + 링크 패널 + 필요 시 복사용 파일
↓
이미지 제작팩
↓
QA
↓
이미지 큐 READY
```

같은 응답에서 이미지 생성 도구를 호출해 글이 사라지게 하지 않습니다.

---

## 11. 이미지 생성

글이 완성된 뒤:

```text
이미지 시작
```

또는

```text
1
```

을 입력합니다.

그다음부터는 `다음`만 입력하면 됩니다.

---

## 12. 체류시간 원칙

체류는 같은 말을 길게 써서 만들지 않습니다.

```text
10초: 핵심 답
30초: 판단 기준
1~2분: 비교·예외·실수 방지
그 이후: FAQ·체크리스트·영상·정보 이미지·내부글
후반: 공식 확인·행동 링크
```

---

## 13. 추천 명령

### 조회수 우선
```text
$naver-blog-master AUTO FULL
지금 조회수를 최대한 노릴 수 있는 주제를 찾아서 v3.8 전체 검증으로 글을 완성해줘.
```

### 링크까지 네이버 복사용
```text
$naver-blog-master AUTO FULL
주제: 근로장려금 반기신청
공식 링크를 실제 검증하고, 사용자에게 보이는 클릭 링크와 네이버 복사용 링크 포함 결과까지 만들어줘.
```

### 글 이후 이미지
```text
이미지 시작
```

---

## 14. 로컬 스킬 업데이트

GitHub 저장소가 v3.8이어도 PC/Codex에 설치된 기존 스킬은 자동 갱신되지 않을 수 있습니다.
최신 저장소를 받은 뒤 설치 스크립트를 다시 실행해 로컬 `SKILL.md`를 업데이트하세요.
